---
const navItems = [
	{
		label: '홈',
		href: '/',
	},
	{
		label: '연락하기',
		href: '/contact',
	},
];
---

<header id="global-nav">
  <div class="container mx-auto"> 
    <nav class="flex justify-between items-center">
      <div class="logo">
        <a href="/" class="flex items-center gap-2">
          <div class="rounded-sm w-10 h-10 overflow-hidden relative">
            <img src="/logo.png" alt="공주쥐 프로필" class="w-full h-full object-cover" />
          </div>
          <span class="text-sm font-bold text-primary-500">쥐라기 | 서울밖 공주쥐와 기록들</span>
        </a>
      </div>
	<ul class="flex gap-4">
		{navItems.map((item) => (
			<li>
				<a href={item.href}>{item.label}</a>
			</li>
		))}
	  </ul>
    </nav>
  </div>
</header>

<style>
  #global-nav {
    padding: 1rem 0.5rem;
    border-bottom: 1px solid var(--color-primary-200);
  }
</style>